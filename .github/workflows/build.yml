name: Build ZMK firmware

on:
  workflow_dispatch:
  push:

jobs:
  build:
    uses: zmkfirmware/zmk/.github/workflows/build-user-config.yml@main
    with:
      build_matrix_path: "config/glove43tb.json"
      config_path: "config"
